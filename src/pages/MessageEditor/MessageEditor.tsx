import { Schema } from "../../types";

export default function MessageEditor({schema}: {
    schema: Schema;
}) {
  return (
    <div className="section">
      <div className="flex-header">
        <h2>Message Editor</h2>
      </div>
    </div>
  );
}
